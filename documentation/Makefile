# GNU Make project makefile autogenerated by Premake

ifndef config
  config=debug
endif

ifndef verbose
  SILENT = @
endif

.PHONY: clean prebuild prelink

ifeq ($(config),debug)
  TARGETDIR = bin/Debug
  TARGET = $(TARGETDIR)/vendor
  define BUILDCMDS
	@echo Running build commands
	mkdocs build -d ../docs
  endef
  define CLEANCMDS
  endef
endif

ifeq ($(config),release)
  TARGETDIR = bin/Release
  TARGET = $(TARGETDIR)/vendor
  define BUILDCMDS
	@echo Running build commands
	mkdocs build -d ../docs
  endef
  define CLEANCMDS
  endef
endif

ifeq ($(config),dist)
  TARGETDIR = bin/Dist
  TARGET = $(TARGETDIR)/vendor
  define BUILDCMDS
	@echo Running build commands
	mkdocs build -d ../docs
  endef
  define CLEANCMDS
  endef
endif

$(TARGET):
	$(BUILDCMDS)

clean:
	$(CLEANCMDS)
